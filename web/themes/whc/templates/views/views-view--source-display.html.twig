<div class="directory">
  <div class="content-header">
    <h1 class="content-header--label">{{title}}</h1>
    <div class="content-header--text">{{header}}</div>
  </div>

  <div class="content-well col-1-3">
    <div>
      <label>Filter</label>
      {{exposed}}
    </div>
    <div class="vdh">
      {{rows}}
      {{empty}}
      {{pager}}
    </div>
  </div>
</div>