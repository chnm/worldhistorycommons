<div class="front-page">
  <div class="featured-banner">
    <div class="content-well">{{header}}</div>
  </div>
  <div class="content-well box-wrapper">

  {% set data = rows.0['#rows']%}
    {% for item in data %}

      <div class="views-row">1</div>

    {% endfor %}

  </div>
  
</div>

{{devel_dump()}}

{{rows}}

{{devel_dump(rows.0['#rows'])}}